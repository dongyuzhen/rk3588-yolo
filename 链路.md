# 基于 V4L2 双队列 + DMABUF 的混合零拷贝链路（按“采集防饥饿”重构版）

> 前提：输入为 **MJPEG**，链路中存在 **软件解码**。目标是在此基础上尽可能减少拷贝，同时保证采集稳定、可长期运行。

---

## 1. 先回答核心问题：是不是“必须”上 CMA 池？

**结论：不是必须。**

应按链路复杂度分两档：

### 档位 A：暂不需要独立 CMA 池（推荐先落地）
适用条件：
- 单路或少分支
- 处理链路较短（解码 -> 预处理 -> 推理 -> 编码）
- 目标是先跑通并稳定

做法：
- V4L2 侧申请缓冲（常见做法：`V4L2_MEMORY_MMAP` + `VIDIOC_EXPBUF` 导出 dmabuf fd）
- 用统一 `Frame` 结构体维护 `index/fd/ptr/ref_count/state`
- 模块间传 `fd + metadata`

### 档位 B：需要独立处理域池（可来自 CMA 池）
适用条件：
- 链路长、多分支（AI + OSD + 编码 + 推流）
- 出现采集饥饿（V4L2 无空闲 buffer，DQBUF 间隔抖动）
- 需要把采集和后处理彻底解耦

做法：
- `DQBUF` 后尽快把数据转移到处理域 dmabuf（`proc_dmabuf`）
- 立刻 `QBUF` 归还 V4L2
- 慢处理在处理域池内部完成

> 重点：**CMA 池是“处理域池”的一种实现，不是唯一实现，更不是必选项。**

---

## 2. 推荐演进路线（从简单到稳定）

### Phase 1（现在就做）
先不引入独立 CMA 池：
1. V4L2 采集并导出 dmabuf fd
2. 统一 `Frame` 生命周期管理（`ref_count + state`）
3. 跑通单链路并打点

### Phase 2（观察是否升级）
当出现以下任意信号时，升级到“采集域/处理域分离”：
- `QBUF` 回收明显滞后，采集帧率不稳
- 队列堆积，端到端延迟持续升高
- 多分支导致 V4L2 buffer 长时间被占用

升级策略：
- 增加 `proc_dmabuf` 池（可用 CMA）
- 执行 `DQBUF -> 转移到 proc -> 立即 QBUF`

---

## 3. 架构重心：采集域短驻留，处理域可堆叠

```text
V4L2(QBUF空闲队列)
  -> 采集写入
V4L2(DQBUF完成队列)
  -> 取出 capture_frame
  -> [可选] 快速转移到 proc_dmabuf
  -> 立即 QBUF 归还 V4L2   <-- 防饥饿关键
  -> RGA/NPU/OSD/MPP 在处理域继续
  -> FFmpeg 推流
```

### 为什么这样设计
- V4L2 是“入口时钟”，应尽量不被后处理拖住
- 后处理模块天然更慢、更不稳定，应该在独立池中消化

---

## 4. 数据结构设计（先做这一步）

建议统一帧对象（示意）：

```cpp
struct Frame {
    uint64_t frame_id;
    uint32_t sequence;

    // 采集域句柄
    int v4l2_index;
    int v4l2_fd;
    void* v4l2_ptr;
    size_t bytes_used;

    // 处理域句柄（Phase 2 再启用）
    int proc_fd;
    void* proc_ptr;

    int width;
    int height;
    int stride;
    uint32_t format;
    uint64_t ts_capture_us;

    std::atomic<int> ref_count;
    enum State { FREE, CAPTURED, TRANSFERRED, PROCESSING, DONE } state;
};
```

说明：
- `Frame` 是生命周期管理对象，不等于必须持有所有像素内存
- 哪个域有效就填哪个句柄，避免过度设计

---

## 5. 生命周期规则（硬约束）

1. `DQBUF` 后：`ref_count = 1`
2. 新增消费分支：`ref_count++`
3. 分支完成：`ref_count--`
4. 仅当 `ref_count == 0` 才允许回收对应 buffer

补充：
- 若仍直接用 V4L2 buffer 走全链路，最终 `ref_count==0` 才 `QBUF`
- 若采用“采集域快速回收”，则在“转移成功后”就 `QBUF`

---

## 6. 同步与一致性（不做会随机出错）

统一原则：
- **设备写 -> CPU读**：`sync for cpu`
- **CPU写 -> 设备读**：`sync for device`

典型场景：
- OpenCV 画框后交给 MPP：必须 `sync for device`
- CPU 读取硬件刚写完的缓冲：先 `sync for cpu`

> 可封装统一接口，禁止业务代码直接散落 ioctl。

---

## 7. 你的代码应怎么写（更细的任务清单 + 构造思路）

下面按“先能跑、再稳、再快”的顺序给出可直接执行的任务。

### Step 1：先定义统一帧对象（必须先做）
**目标：全链路只传一个 `Frame` 句柄，避免各模块各管一套。**

建议字段：
- 基础标识：`frame_id / sequence / state`
- V4L2句柄：`v4l2_index / v4l2_fd / v4l2_ptr / bytes_used`
- 处理域句柄（预留）：`proc_fd / proc_ptr`
- 图像属性：`width/height/stride/format`
- 时序：`ts_dqbuf, ts_decode_done, ts_rga_done, ts_npu_done, ts_encode_done, ts_push_done`
- 生命周期：`std::atomic<int> ref_count`

落地要求：
1. `Frame` 由采集模块创建和回收。
2. 下游模块只“借用”，不得擅自 close fd。
3. 所有线程之间传 `Frame*`（或 `shared_ptr<Frame>`），不传裸像素块。

### Step 2：把采集线程写成“单一职责”
**目标：只负责 `DQBUF/QBUF` 与 metadata 填充，不做重计算。**

实现动作：
1. `poll -> DQBUF` 拿到驱动完成帧。
2. 填充 `Frame`：`bytes_used/sequence/flags/timestamp/index`。
3. `ref_count = 1`，`state = CAPTURED`。
4. 推入 `capture_q`。

注意：
- 采集线程不要做 decode、推理、编码，避免阻塞入口。

### Step 3：先实现“直通版本”（不启用 proc 池）
**目标：先跑通端到端，验证功能正确与同步正确。**

流程：
- `capture_q -> decode/rga/npu/encode -> 完成后 QBUF`

实现动作：
1. 在处理线程中使用 `v4l2_ptr` 做 MJPEG 解码。
2. 后续按你当前模块串起来（RGA/NPU/MPP/FFmpeg）。
3. 帧结束时 `ref_count--`，当 `==0` 时 `QBUF`。

### Step 4：加可观测性（决定要不要上 CMA 池的依据）
**目标：用数据驱动升级，而不是主观感觉。**

每帧至少打印/统计：
- `dqbuf_interval_ms`
- `decode_ms`
- `rga_ms`
- `npu_ms`
- `encode_ms`
- `e2e_ms`
- `capture_queue_depth / process_queue_depth`

触发升级条件（任一满足）：
1. `dqbuf_interval` 抖动持续偏大（采集节奏不稳）；
2. 多次出现“可QBUF数量过低/无空闲采集buffer”；
3. `e2e p95` 持续超预算。

### Step 5：出现饥饿后再启用处理域池（可用 CMA）
**目标：采集域与慢处理解耦，保证采集不断流。**

构造思路：
1. 新增 `proc_dmabuf[N]`（来源可为 CMA 池）。
2. 在 decode 后或 rga 前，把数据转移到 `proc_dmabuf`。
3. 转移成功后立刻 `QBUF(v4l2_index)`。
4. 后续慢处理只操作 `proc_fd/proc_ptr`。

关键点：
- 这是“用一次可控转移换采集稳定”。
- 一旦进入该模式，V4L2 buffer 生命周期应显著变短。

### Step 6：多分支时加引用计数规则
**目标：AI/编码/OSD 并行时不提前回收。**

规则固定：
1. 分支入口 `ref_count++`。
2. 分支结束 `ref_count--`。
3. `ref_count==0` 的线程负责最终回收（QBUF 或归还 proc 池）。

建议：
- 用 RAII 小对象（如 `FrameRefGuard`）自动 `--ref_count`，避免遗漏。

### Step 7：把 cache sync 做成统一接口
**目标：避免随机花屏、旧数据、偶发错帧。**

接口建议：
- `sync_for_cpu(fd)`
- `sync_for_device(fd)`

调用规范：
1. 硬件写完给 CPU 读前：`sync_for_cpu`。
2. CPU 改写后给硬件读前：`sync_for_device`。
3. 禁止业务代码直接散落 ioctl，统一走封装。

### Step 8：异常与背压策略（上线前必须）
**目标：链路长期运行不雪崩。**

必须具备：
1. 队列高水位告警与丢帧策略（建议丢旧保新）。
2. 失败重试与限流（RGA/NPU/编码失败计数）。
3. 推流阻塞时的降级策略（降帧/降分辨率/只本地处理）。
4. 关键资源泄漏检查（fd 数、队列长度、buffer state）。

---

## 8. 伪代码（包含“快速回收”分支）

```cpp
void capture_thread() {
    while (running) {
        Frame* f = dqbuf_v4l2();
        f->ref_count.store(1);
        f->state = CAPTURED;
        capture_q.push(f);
    }
}

void decode_transfer_thread() {
    while (running) {
        Frame* f = capture_q.pop();

        // MJPEG 软件解码（读 v4l2_ptr）
        // decode_mjpeg(...)

        if (need_fast_requeue) {
            // 转移到处理域（可来自 CMA 池）
            // copy_or_rga_blit(v4l2 -> proc)
            // sync_for_device(proc_fd)

            qbuf_v4l2(f->v4l2_index);   // 关键：尽快归还采集buffer
            f->state = TRANSFERRED;
        }

        process_q.push(f);
    }
}

void infer_encode_thread() {
    while (running) {
        Frame* f = process_q.pop();

        // rga + npu + mpp + ffmpeg

        if (f->ref_count.fetch_sub(1) == 1) {
            if (!need_fast_requeue) {
                qbuf_v4l2(f->v4l2_index);
            }
            f->state = FREE;
        }
    }
}
```

---

## 9. 风险与规避清单

1. **采集饥饿**：V4L2 buffer 被后处理长时间占用  
   - 规避：启用“处理域转移 + 立即QBUF”

2. **格式/stride 不匹配**：RGA/NPU/MPP 约束不同  
   - 规避：池创建时统一对齐策略

3. **cache sync 漏做**：偶发花屏/错帧  
   - 规避：统一 sync 封装 + 关键路径断言

4. **提前回收**：撕裂与随机崩溃  
   - 规避：`ref_count==0` 才回收

5. **队列积压**：延迟无限增长  
   - 规避：高水位 + 丢旧保新 + 背压监控

---

## 10. 最终建议（一句话）

**你现在不必强上 CMA 池。先用 V4L2 dmabuf + Frame 生命周期把链路跑稳；只有在出现采集饥饿/长链路堆积时，再引入处理域池（可用 CMA）并执行“早转移、早回收”。**
