# 执行清单（文件/函数级修改项）

按建议执行顺序列出，方便逐步落地。

---

## 1) `camera/cma_buffer.h`

**新增写同步接口**（避免 CPU 写后硬件读脏数据）

在 `class CmaBuffer` 中新增：
- `bool syncStartWrite() const;`
- `bool syncEndWrite() const;`
- （可选）`bool syncStartReadWrite() const;`
- （可选）`bool syncEndReadWrite() const;`

用途：OpenCV 画框、CPU 预处理后提交给 RGA/NPU/MPP。

---

## 2) `camera/cma_buffer.cpp`

**实现写同步接口**（使用 `DMA_BUF_IOCTL_SYNC`）

新增实现：
- `syncStartWrite()` → `DMA_BUF_SYNC_START | DMA_BUF_SYNC_WRITE`
- `syncEndWrite()` → `DMA_BUF_SYNC_END | DMA_BUF_SYNC_WRITE`
- （可选）`READ_WRITE` → `DMA_BUF_SYNC_RW`

---

## 3) `camera/v4l2_camera.h`

**扩展 `FramePacket` 元数据**

在 `FramePacket` 里新增字段：
- `uint32_t sequence{0};`  （来自 `v4l2_buffer.sequence`）
- `uint32_t flags{0};`      （来自 `v4l2_buffer.flags`）
- （可选）`uint32_t field{0};`

用途：顺序校验 / 异常帧判断 / 调试定位。

---

## 4) `camera/v4l2_camera.cpp`

**捕获与回收路径增强**

### A. `captureFrame()`
- 取出 `buf.sequence / buf.flags / buf.field` 填入 `FramePacket`
- 若 `buf.flags` 标记错误帧，则记录日志并按需丢帧

### B. `releaseFrame()`
- 保持现有 `syncEndRead()`
- 若后续存在“CPU 写后回收”路径，可切换 `syncEndWrite()`

### C. （可选）异常恢复策略
- `poll` 返回异常 → 打印错误 + 退出/重启流

---

## 5) `main.cpp`

**接入双队列与生命周期管理（最小框架）**

### A. 数据结构扩展
- 增加 `frame_id`
- 增加 `ref_count`（原子）
- 透传 `sequence`

### B. capture 线程
- `DQBUF` 后 `ref_count=1`
- 入 `capture_ready_q`

### C. process / infer 线程
- 不回收，仅 `ref_count--`

### D. encode 线程
- `ref_count==0` 时 `releaseFrame()`

---

## 最小落地路径（建议顺序）

1. `cma_buffer.h/.cpp`（写同步接口）
2. `v4l2_camera.h/.cpp`（metadata + 捕获/回收增强）
3. `main.cpp`（双队列 + ref_count 流程）
