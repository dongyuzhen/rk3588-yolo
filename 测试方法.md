# 项目调试方法（面向 V4L2 + RGA + NPU + VPU 流水线）

> 目标：不是“修一个 bug”，而是建立一套可复用的调试思路，快速定位问题、验证假设、沉淀经验。

---

## 1. 先建立“可调试”工程习惯

在进入具体问题前，先把工程改造成“容易看见真相”的状态。

### 1.1 统一日志规范（强烈建议）

建议至少定义 4 级日志：`DEBUG / INFO / WARN / ERROR`，并且每条日志包含：

- 时间戳（最好到 ms）
- 线程 ID
- 模块名（CAMERA/RGA/NPU/VPU/POST）
- 关键上下文（帧号、buffer index、耗时）

示例：

- `[12345.678][TID:1024][CAMERA][INFO] frame=356 dequeue buf=2 latency=11.2ms`
- `[12345.689][TID:1033][NPU][WARN] frame=356 infer timeout 50ms`

这样后面做时序排查会非常高效。

### 1.2 每帧唯一 ID（Frame ID）贯穿全链路

给每帧分配递增 ID，并在每个阶段打印同一个 ID：

- 采集（V4L2）
- 预处理（RGA）
- 推理（NPU）
- 后处理（post_process）
- 编码/显示（VPU/Display）

有了 frame id，你能一眼看出：

- 哪一帧丢了
- 哪一步卡住了
- 是否发生乱序

### 1.3 关键指标“默认可见”

建议把以下指标固定打印（每秒一次汇总，避免刷屏）：

- FPS（输入/推理/输出）
- 各阶段平均耗时 + P95/P99
- 队列长度（生产者-消费者模型）
- 丢帧计数
- 超时计数

---

## 2. 分层调试法：按模块拆解，不要一把梭

你这个项目是典型多模块流水线，调试顺序建议是：

1. **单模块自测**（输入可控）
2. **两两联调**
3. **全链路压测**

---

## 3. V4L2 采集层调试

常见问题：无画面、花屏、卡顿、延迟高、偶发超时。

### 3.1 先验证设备能力与参数一致性

检查项：

- 分辨率、像素格式（如 YUYV/NV12）
- 帧率是否真正生效
- 驱动支持的 buffer 数量

调试要点：

- 启动时把所有协商后的参数完整打印
- 若设置失败，打印 errno 和 ioctl 返回值

### 3.2 DQBUF/QBUF 问题定位

你已做了 DQBuffer 轮询优化，这里重点看：

- `DQBUF` 平均等待时间
- `DQBUF` 超时次数
- `QBUF` 是否及时回收
- 队列是否被“吃空”或“堆满”

典型异常：

- **DQBUF 经常超时**：可能摄像头输出异常 / 帧率设置过高 / 下游处理堵塞导致 QBUF 不及时。
- **缓冲越来越少可用**：通常是某路径丢了回收（异常分支忘记 QBUF）。

### 3.3 采集正确性验证

做法：

- 周期性将原始帧保存到文件（例如每 100 帧保存 1 帧）
- 离线查看图像是否正常（亮度、色彩、裁切）

这一步能快速区分：

- 是摄像头输入坏了
- 还是后续预处理/推理导致的“看起来坏”

---

## 4. CMA / DMA 内存与零拷贝路径调试

常见问题：偶发崩溃、地址非法、性能抖动大。

### 4.1 内存分配与释放配对检查

对每个 buffer 打印：

- 物理地址 / fd / 虚拟地址
- 分配时间、释放时间
- 引用计数（如果有）

目标：避免

- 重复释放
- 漏释放
- 释放后继续使用（UAF）

### 4.2 边界与对齐问题

图像处理常见坑：stride、对齐、平面偏移。

建议检查：

- width/height 与 stride 的关系
- UV 平面起始偏移
- RGA/NPU 输入要求对齐（16/64 字节等）

如果图像出现“斜纹/错位/颜色异常”，第一时间怀疑 stride 或 format 映射。

---

## 5. RGA 预处理调试（色彩转换/缩放）

常见问题：颜色偏差、目标拉伸、推理精度骤降、耗时异常。

### 5.1 输入输出一致性验证

建议同时保存：

- RGA 输入帧（源）
- RGA 输出帧（送推理前）

然后做离线比对：

- 分辨率是否符合模型输入
- 是否保持纵横比（是否做了 letterbox）
- 色彩空间是否正确（YUV->RGB/BGR 顺序）

### 5.2 性能验证

你提到 RGA 每帧约 6ms，建议进一步细化：

- 单次调用耗时分布（均值、最大值）
- 峰值是否与系统负载相关
- 批量连续执行时是否退化

如果有偶发 20ms+，重点看：

- 内存带宽争用
- 输入 buffer 是否跨 NUMA/不连续（若平台有相关差异）
- 同时运行模块是否抢占严重

---

## 6. NPU 推理层调试（RK3588 NPU / YOLOv5）

常见问题：无检测框、框位置错误、置信度异常、性能掉速。

### 6.1 输入张量正确性

在推理前打印一次（或周期采样）：

- 输入 shape / dtype / layout（NCHW/NHWC）
- 归一化参数（scale/mean/std）
- 实际最小值/最大值（避免全 0、全 255）

很多“模型失效”不是模型问题，而是预处理和训练时不一致。

### 6.2 输出张量排查

输出后先不做后处理，直接看原始统计：

- 每个输出 tensor 的 shape
- 前几个值范围
- 是否出现 NaN/Inf

如果全是 0 或全很小，优先排查：

- 模型版本/量化参数不匹配
- 输入通道顺序错误
- 量化反量化参数错误

### 6.3 性能调试

建议拆成三段计时：

- `set_input`
- `run`
- `get_output`

你当前单核 12 FPS、多核 40 FPS，可继续观察：

- 核心绑定策略是否稳定
- 多线程喂数据是否造成锁竞争
- 是否有隐含同步点拖慢流水线

---

## 7. 后处理调试（NMS / 坐标映射）

你项目里后处理是核心精度风险点。

### 7.1 坐标映射检查（最常出错）

若有 letterbox，需检查：

- 缩放比例 `scale`
- 填充偏移 `pad_x / pad_y`
- 反算到原图坐标是否越界

建议写一个“已知输入 -> 期望输出”的小单测：

- 人工构造一个固定框
- 跑映射函数
- 校验与手算结果一致

### 7.2 NMS 可视化验证

调试技巧：

- NMS 前后框数量都打印
- 置信度阈值、IoU 阈值打印到日志
- 把 NMS 前后结果画框导出图像，肉眼对比

如果出现“框太多/框全没了”，通常是阈值或类别过滤逻辑错误。

---

## 8. 多线程流水线调试（生产者-消费者 + Future/Promise）

常见问题：死锁、乱序、吞吐不稳定、内存涨。

### 8.1 队列与背压

每个队列都记录：

- 当前长度
- 峰值长度
- 入队速率/出队速率

现象与结论：

- 队列持续增长：下游处理能力不足
- 队列经常空：上游供给不足或抖动大

### 8.2 死锁/卡住定位

做法：

- 每个线程定期“心跳日志”（例如每 1s 打印一次状态）
- 对锁等待超时进行告警
- 关键等待点（future.get、condition_variable.wait）加超时版本

一旦卡住，可快速知道“哪个线程最后活着、卡在哪”。

### 8.3 乱序与时延扩散

借助 frame id 和时间戳画出简易时序：

- 帧 100 在各阶段的到达时间
- 帧 101 是否先于 100 输出

如果允许乱序，需明确策略；如果不允许，需在汇聚点排序并统计延迟代价。

---

## 9. 性能调试方法（从“感觉慢”到“证据慢”）

### 9.1 建立基准场景

固定以下条件再做对比：

- 输入视频源
- 分辨率/帧率
- 模型版本
- 线程数
- CPU/NPU 频率策略

没有基准，优化结论不可信。

### 9.2 分阶段打点（必做）

对每帧记录：

- t_capture_in/out
- t_rga_in/out
- t_npu_in/out
- t_post_in/out
- t_render_or_encode_out

然后计算：

- 单阶段耗时
- 端到端耗时
- 抖动（标准差 / P99）

### 9.3 做 A/B 实验

例如：

- A：CPU 预处理
- B：RGA 预处理

比较：FPS、延迟、CPU 占用、精度变化。只改一个变量，避免干扰。

---

## 10. 稳定性调试（长稳压测）

仅跑 5 分钟稳定不代表上线稳定，建议：

- 连续运行 2h / 8h / 24h
- 每 10s 打印一次系统资源
- 记录异常计数（超时/丢帧/重试）

重点观察：

- 内存是否缓慢上涨（泄漏）
- FPS 是否逐步下滑（热降频或队列积压）
- 是否出现“长时间后第一次崩溃”

---

## 11. 崩溃类问题调试（Segfault/Abort）

### 11.1 编译选项建议

调试版本建议打开：

- `-g`（符号）
- `-O0` 或 `-Og`（便于调试）
- `-fno-omit-frame-pointer`

### 11.2 使用 gdb 定位

核心命令：

- `run`
- `bt`（回溯）
- `frame N`
- `info locals`
- `print 变量`

先拿到“第一现场调用栈”，再回看日志里的 frame id，通常就能闭环。

### 11.3 Sanitizer（如果平台支持）

优先尝试：

- AddressSanitizer：越界/UAF
- UndefinedBehaviorSanitizer：未定义行为
- ThreadSanitizer：数据竞争（多线程）

即使不能全量在板端开，也可以在 x86 模拟逻辑层先跑。

---

## 12. 图像算法问题的专项调试

当“程序没崩，但结果不对”时，采用三件套：

1. **中间结果落盘**（输入图、预处理图、画框图）
2. **关键参数打印**（阈值、缩放、偏移、类别 id）
3. **离线回放**（固定同一帧反复调）

这样可以把实时系统问题变成离线确定性问题，调试效率会高很多。

---

## 13. 建议你养成的“调试思维”

1. **先复现，再修复**：不能稳定复现的问题，不要急着改。
2. **先缩小范围**：先判断是采集、预处理、推理还是后处理。
3. **先证据后结论**：任何猜测都要用日志/图像/指标验证。
4. **一次只改一个变量**：避免把问题“改没了也说不清”。
5. **保留最小复现样例**：以后同类问题可快速回归。

---

## 14. 一个实战排查模板（可直接套用）

当你遇到“检测框漂移 + FPS 下降”时，按这个顺序：

1. 看汇总日志：是哪一阶段耗时上升。
2. 抽取同一 frame id 的全链路日志：定位延迟扩散点。
3. 导出该帧的原图 + RGA 输出图 + 最终画框图：确认是视觉变换问题还是后处理问题。
4. 打印该帧的输入 tensor 统计和输出 tensor 统计：排除 NPU 输入异常。
5. 单独跑后处理单测（固定输出张量）：确认映射/NMS 是否正确。
6. 做 A/B（关闭某优化项）：验证是否某次优化引入副作用。

---

## 15. 你可以立即实施的 5 个升级动作

1. 给每帧加全链路 frame id。
2. 建立每秒性能汇总日志（含 P95/P99）。
3. 增加“中间图像落盘开关”。
4. 所有阻塞等待改为“带超时 + 告警”。
5. 建一个最小离线回放程序专测后处理。

---

如果你愿意，我下一步可以继续帮你做两件非常实用的事：

- 按你当前代码结构（`main.cpp`、`camera/v4l2_camera.cpp`、`yolo/post_process.cpp`）给出**具体埋点位置清单**；
- 直接给你一版**可拷贝的日志宏与性能统计器**（低侵入、适合 C++11）。

对，可以用 perf stat，但不要只用一个指标。
要体现 DMABUF 提效，建议做“同场景 A/B 对比”：
• A：普通路径（例如 mmap + 额外拷贝）
• B：DMABUF 路径
然后固定：
• 分辨率/帧率（如 1280x720@30）
• 帧数（如 1000）
• 是否推流一致
• 运行时长一致
￼
推荐指标（最有说服力）
1. CPU 使用/周期
perf stat -e task-clock,cycles,instructions,context-switches,cpu-migrations ./app ...
2. 缓存/内存行为
perf stat -e cache-references,cache-misses,page-faults,minor-faults,major-faults ./app ...
3. 业务指标
• FPS
• 单帧耗时（mean/p95）
• 丢帧率
￼
怎么解释“提效”
如果 DMABUF 生效，常见表现是：
• task-clock 下降（CPU时间更少）
• cycles 下降
• cache-misses 下降或 miss rate 下降
• page-faults 更平稳
• 在同 CPU 占用下 FPS 更高，或同 FPS 下 CPU 更低
￼
一条可直接用的命令（示例）
perf stat -e task-clock,cycles,instructions,cache-references,cache-misses,page-faults,minor-faults,major-faults \  
./app --no-rtmp
跑两次（A/B）后做对比表就行。