# 项目总结（RK3588 实时视觉流水线）

## 1. 目标与范围

本项目面向 RK3588 平台，构建一条可长期稳定运行的实时处理链路，完成：

- 摄像头采集（V4L2）
- 目标检测（YOLOv5 + RKNN/NPU）
- 视频编码（MPP）
- 可选网络推流（RTMP）
- 采集时延与系统指标量化展示

本文重点是“可复现、可量化、可维护”的工程化总结。

---

## 2. 总体架构（输入-处理-输出）

### 2.1 输入（Input）

- 设备：`/dev/video0`
- 接口：V4L2 Streaming
- 格式：MJPEG
- 缓冲：DMABUF（CMA 连续内存）

### 2.2 处理（Process）

1. **Read 线程（采集）**
   - `poll` 等待帧到达
   - `VIDIOC_DQBUF` 取帧
   - `DMA_BUF_IOCTL_SYNC(START|READ)`
   - `cv::imdecode` 解码 MJPEG -> BGR
   - 统计 `poll_ms/dqbuf_ms/total_ms`
   - `releaseFrame` 回收缓冲（`SYNC END + QBUF`）

2. **Aggregator 线程（并行推理 + 按序聚合）**
   - 从读队列取帧并异步提交线程池
   - 维护 `map<index, future>`
   - 严格按 `nextWriteIndex` 顺序输出

3. **Write 线程（颜色转换 + 编码/推流）**
   - BGR -> NV12（RGA 硬件）
   - MPP 编码
   - RTMP 可选推流

### 2.3 输出（Output）

- 本地编码文件（如 `output.avi`）
- 可选 RTMP 流
- 量化报告（FPS、单帧周期、缺页统计、采集时延分布）

---

## 3. 框架图

```text
┌──────────────┐
│  /dev/video0 │
└──────┬───────┘
       │ V4L2 + DMABUF(CMA)
       v
┌──────────────┐       CaptureMetrics
│ Read Thread  │──────(poll/dqbuf/total)
└──────┬───────┘
       │ g_readQueue
       v
┌──────────────┐
│ Aggregator   │ -> ThreadPool -> YOLOv5(NPU)
└──────┬───────┘
       │ g_writeQueue(按序)
       v
┌──────────────┐
│ Write Thread │ -> RGA(BGR->NV12) -> MPP -> RTMP(可选)
└──────┬───────┘
       v
   文件/流输出
```

---

## 4. 函数级流程（可直接对照代码实现）

### 4.1 启动阶段（`main`）

1. 解析参数（`--no-rtmp`）
2. `g_camera.init(...)`
3. `g_camera.startStreaming()`
4. `init_streamer(...)`
5. 启动三个线程：`Read_video` / `aggregatorThreadFunc` / `writeThreadFunc`

### 4.2 采集阶段（`Read_video` + `V4L2Camera`）

每轮执行：

1. `g_camera.captureFrame(packet, timeout)`
   - 返回 `FramePacket{data, bytes_used, buffer_index, timing...}`
2. `cv::imdecode(...)` 将 MJPEG 转 BGR
3. 入队：`g_readQueue.enqueue(...)`
4. 统计：`g_capture_metrics.add(packet.timing)`
5. 回收：`g_camera.releaseFrame(packet)`

> 关键点：`captureFrame` 与 `releaseFrame` 必须成对调用，否则缓冲会耗尽。

### 4.3 推理聚合阶段（`aggregatorThreadFunc`）

1. 从读队列取帧并 `submit_task_async`
2. 以 `future` 非阻塞检查完成状态
3. 按序写入 `g_writeQueue`

### 4.4 编码输出阶段（`writeThreadFunc`）

1. `BGR_to_NV12(...)`（RGA）
2. `process_frame(...)`（MPP 编码路径）
3. 按配置决定是否推 RTMP

### 4.5 退出阶段（`main`）

1. `join` 三个线程
2. 停止队列
3. 释放 writer/streamer
4. `g_camera.stopStreaming()` + `g_camera.closeDevice()`
5. 打印最终采集统计

---

## 5. 核心优化项与原因（按截图要求）

### A. 采集模块对象化（`camera/v4l2_camera`）

**做法**：将设备初始化、队列管理、采集、回收封装为对象接口。  
**原因**：降低主流程耦合，问题定位更快，后续扩展更容易。

### B. DMABUF + CMA 内存路径

**做法**：从 `/dev/dma_heap/cma` 申请 DMABUF，走 `V4L2_MEMORY_DMABUF`。  
**原因**：减少不必要拷贝与内存抖动，提升长时间稳定性。

### C. DMA 同步保证一致性

**做法**：读取前 `SYNC START`，回收前 `SYNC END`。  
**原因**：保证设备与 CPU 缓冲访问一致，降低花屏/脏帧风险。

### D. 三线程流水并行 + 按序输出

**做法**：采集/推理/编码解耦，并发处理，最终按 index 有序输出。  
**原因**：提升吞吐，同时保证结果顺序可控。

### E. 采集时延量化

**做法**：持续统计 `poll/dqbuf/total` 的 `mean/p50/p95/max`。  
**原因**：便于发现“平均正常但 P95 抖动大”的真实问题。

---

## 6. 量化成果展示方法（不做阈值比对）

### 6.1 采集吞吐与单帧周期

```bash
./tools/show_quant_results.sh /dev/video0 300
```

输出包含：

- FPS
- 单帧采集周期（近似）

### 6.2 缺页中断与命中率近似

同一脚本会输出：

- `page-faults`
- `minor-faults`
- `major-faults`
- `hit_rate = 1 - page_faults/frames`（近似指标）

> 说明：当使用系统级口径时，结果会受其他进程干扰，适合做“趋势观察”。

---

## 7. 自动化入口

### 7.1 一键构建 + 热身 + 报告

```bash
./tools/auto_quant.sh /dev/video0 300
```

自动完成：

1. CMake 配置（HARD）
2. 编译
3. app 热身运行
4. 输出量化报告

---

## 8. 当前交付状态

- 已具备完整“输入-处理-输出”实现链路
- 已具备函数级流程说明与模块职责划分
- 已具备量化展示脚本与自动化入口
- 已具备核心模块注释（`main/camera/pool/SafeQueue`）

该版本可用于：课程汇报、项目答辩、阶段验收材料。

命中率代表什么？
缓存命中率表示 CPU 访问数据时，有多少比例能在缓存里直接拿到（不需要去更慢的内存）。
• 命中率高：CPU 等待内存的时间更少，通常更高效
• 命中率低：更多访问落到内存，延迟更高，性能更差
你这次 98.73% 属于比较好的水平。
不过要注意：这通常是整体 cache 统计，不是只看某一个模块（比如仅 DMABUF）的纯指标。